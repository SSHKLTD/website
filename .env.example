# Notion CMS sync (optional — the site builds from committed JSON when absent)
# Create an internal integration at https://www.notion.so/my-integrations,
# then share the three databases with it (案例庫 / 客戶索引 / 招聘庫).
NOTION_TOKEN=

# Contact form email delivery via Resend (functions/api/contact.ts).
# Set in the Cloudflare Pages dashboard, not in this repo.
RESEND_API_KEY=
CONTACT_TO=info@sshk.ltd
